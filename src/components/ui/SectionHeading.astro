---
interface Props {
  as?: "h1" | "h2" | "h3";
  class?: string;
}

const { as: Tag = "h2", class: className } = Astro.props;
---

<Tag class:list={["text-sm font-bold mb-4 text-muted", className]}>
  <slot />
</Tag>
