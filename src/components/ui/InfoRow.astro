---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div class="flex flex-col sm:flex-row sm:items-center gap-0.5 sm:gap-0">
  <span class="text-muted sm:w-36 shrink-0">{label}</span>
  <span class="text-secondary"><slot /></span>
</div>
